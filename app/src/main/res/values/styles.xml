<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- アプリのテーマ -->
    <style name="AppTheme" parent="android:Theme.Black.NoTitleBar">
        <item name="android:buttonStyle">@style/StandardButtonTheme</item>
    </style>

    <!-- Theme - MainLayout -->
    <style name="StandardViewStyle" parent="AppTheme">
        <item name="android:background">@color/background</item>
        <item name="android:padding">@dimen/view_padding</item>
    </style>

    <!-- Theme - Button -->
    <style name="StandardButtonTheme" parent="android:Widget.Button">
        <item name="android:textColor">@color/button_text</item>
        <item name="android:textSize">@dimen/button_text_size</item>
        <item name="android:background">@drawable/standard_button_background</item>
    </style>

    <!-- Style - Spinner -->
    <style name="StandardSpinnerStyle" parent="android:Widget.Spinner">
        <item name="android:background">@drawable/spinner_background</item>
        <item name="android:paddingTop">@dimen/spinner_padding</item>
        <item name="android:paddingBottom">@dimen/spinner_padding</item>
        <item name="android:paddingLeft">@dimen/spinner_padding</item>
        <item name="android:paddingRight">@dimen/spinner_padding_right</item>
    </style>
</resources>
